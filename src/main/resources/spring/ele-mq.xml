<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
    http://www.springframework.org/schema/rabbit
    http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd">

    <!--配置application-mq.xml配置文件,声明连接,交换机,queue以及consumer监听.-->
    <description>rabbitmq 连接服务配置</description>

    <rabbit:connection-factory id="connectionFactory" host="${mq.host}" username="${mq.username}"
                               password="${mq.password}" port="${mq.port}" virtual-host="${mq.vhost}"/>

    <context:component-scan base-package="com.ximalaya.rabbitmq.helloworld"/>

    <!-- spring template声明-->
    <rabbit:template exchange="amqpExchange" id="amqpTemplate" connection-factory="connectionFactory"/>

    <!--申明queue-->
    <rabbit:queue id="meepo.statistic.subsidy"  name="meepo.statistic.subsidy" durable="true" auto-delete="false" exclusive="false"/>

    <bean id="messageListenerDemo" class="com.ele.rabbitmq.helloworld.MessageListenerDemo"></bean>

    <!--consumer配置监听-->
    <!-- queue listener  观察 监听模式 当有消息到达时会通知监听在对应的队列上的监听对象 taskExecutor这个需要自己实现一个连接池 按照官方说法 除非特别大的数据量 一般不需要连接池-->
    <rabbit:listener-container connection-factory="connectionFactory" acknowledge="auto">
        <rabbit:listener queues="meepo.statistic.subsidy" ref="messageListenerDemo"/>
    </rabbit:listener-container>
</beans>